<h1><%= @property.name %><%= CheckOption.model_name.human %></h1>

<table>
  <tr>
    <th><%= CheckOption.human_attribute_name(:item_name) %></th>
    <th></th>
    <th></th>
  </tr>

<% @check_options.each do |check_option| %>
  <tr>
    <td><%= check_option.item_name %></td>
    <td><%= link_to 'Edit', edit_check_option_path(check_option) %></td>
    <td><%= link_to 'Destroy', check_option, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '新建选项', new_check_option_path(:property_id => @property.id) %>
